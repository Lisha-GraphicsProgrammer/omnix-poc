# Python
__pycache__/
*.pyc
venv/
.venv/

# Env & secrets
.env

# OS
.DS_Store
Thumbs.db

# Datasets & training junk (never commit these)
vest_dataset/
helmet_dataset/
hard_hat_universe/
Forklift-2/
*.zip

# Videos (test assets stay local)
*.mp4
!mega_cctv.mp4

# Model weights & training runs (new ones; existing tracked files unaffected)
runs/detect/*/weights/last.pt
runs/detect/*/*.png
runs/detect/*/*.jpg

# Pipeline artifacts
incidents/
incidents.json
pipeline_config.json
pipeline_config.backup.json

# Training run debris
runs/detect/predict*/
runs/detect/*/args.yaml
runs/detect/*/results.csv
test_summary.json
test_frame_*.jpg

# Self-learning pipeline (new datasets/models — never commit these)
datasets/
runs/self_learning/
body.json