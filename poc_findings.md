\# PoC Findings — OMNIX LLM to YOLO Connection Test



\## What worked

\- LLM converted all 3 plain English instructions to valid JSON correctly

\- Truck detection works end to end — 564 trucks detected

\- Person detection works — 196 persons detected

\- Count logic correctly generated by LLM: COUNT(person\_within\_zone) > 5

\- Core loop confirmed: Instruction → LLM → JSON → YOLO → Detection



\## What broke / gaps found

\- Helmet class NOT in standard YOLOv8 (COCO dataset)

&#x20; Result was PARTIAL — person detected but helmet could not be checked

&#x20; Solution: Fine-tune YOLOv8 on helmet dataset (available free on Roboflow)



\## Key insight

\- YOLO detects objects only

\- Zone logic, count threshold, rule execution must be in our pipeline code

\- LLM correctly generated these rules in JSON — our code must read and apply them



\## Next step recommendation

\- Download helmet dataset from Roboflow

\- Fine-tune YOLOv8 on helmet data — 1 day task

\- Build rule executor that reads JSON and applies count and zone logic

\- PoC confirms core loop is viable — proceed to SRS phase

